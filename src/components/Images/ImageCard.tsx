import { Image } from '../../types';

interface IImage {
  image: Image,
  key: number
}

const ImageCard = ({key, image} : IImage) => {

  return (
    <article>
      
    </article>
  )
}

export default ImageCard
